# 弱转强准入门槛 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `W2S01≥gate` 做成活跃池准入硬门槛(涨幅≥5% AND 弱转强成立),使进入综合分排序的每只候选都先通过弱转强检验。

**Architecture:** 在 `IntegratedDataFetcher._filter_active_from_kline` 的涨幅筛选后、`_rank_by_leader_strength` 排序前,插入新方法 `_filter_by_w2s01_gate`。该方法仿已验证的 `_compute_volume_ratio` 逐票取过去 L+1 日 K 线,复用 `DailyW2SFactors.calculate_W2S01`(已合入 main 的改进版)算 W2S01,保留 ≥gate 的票;窗口不足/None/查询失败的票剔除。

**Tech Stack:** Python 3.9(系统解释器,无 venv 无 pytest);测试用 standalone `python3 tests/xxx.py`;sqlite3 直连(仿 `_compute_volume_ratio` 的 busy_timeout 模式)。

## Global Constraints
- 语言:Python 3.9 兼容(避免 `match`、`X | Y` 联合类型)。
- 接入点唯一:`data_fetcher_v3_integrated.py:_filter_active_from_kline`,`candidates = df[df['change_pct'] >= min_change_pct]`(455 行)之后、`_rank_by_leader_strength`(458)之前。
- 复用 `DailyW2SFactors.calculate_W2S01`(准入判据与下游因子分同一套弱转强定义)。
- gate 配置化:`QSS_W2S01_GATE` 环境变量,默认 50(仿现有 `QSS_LEADER_W_*` 惯例)。
- 窗口不足(<L+1 日)/ W2S01 为 None / 单票查询失败 → 剔除该票;整库无路径 → 跳过门槛 + 警告。
- 局部 import(`import os`、`from factors.w2s_daily_factors import DailyW2SFactors`)避免顶部循环 import(项目惯例见 527/1346)。
- 提交 `_bmad-output` 在 .gitignore,用 `git add -f`;commit message 结尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

### Task 1: `_filter_by_w2s01_gate` 方法 + 配置 + 接入

**Files:**
- Modify: `data_fetcher_v3_integrated.py`(`IntegratedDataFetcher.__init__` 末尾加配置/实例;新增 `_filter_by_w2s01_gate` 方法,置于 `_compute_volume_ratio` 之后;`_filter_active_from_kline` 接入)
- Test: `tests/test_w2s01_gate.py`(新建,真实 DB 集成测试)

**Interfaces:**
- Consumes: `DailyW2SFactors.calculate_W2S01(today_data, yesterday_data)`(已合入 main);`stock_kline` 表(列 close_price/volume/ma20/ma5/change_pct/trade_date)。
- Produces: `_filter_by_w2s01_gate(self, candidates: pd.DataFrame, trade_date: str, gate: float) -> pd.DataFrame`:返回 `candidates` 中 W2S01≥gate 的子集(保序);`self.w2s01_gate`(float);`self._w2s_calc`(DailyW2SFactors 实例)。

- [ ] **Step 1: 写失败测试** `tests/test_w2s01_gate.py`

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from data_fetcher_v3_integrated import IntegratedDataFetcher
import pandas as pd

f = IntegratedDataFetcher()

def test_gate_filters_pseudo_weak():
    # 0626 今日Top5:605366/600520/002668 为伪弱(W2S01<50),603977/002225 真弱转强(≥50)
    codes = ['605366', '600520', '002668', '603977', '002225']
    cand = pd.DataFrame({'stock_code': codes, 'change_pct': [10.0, 7.5, 7.0, 10.0, 6.9]})
    out = f._filter_by_w2s01_gate(cand, '20260626', gate=50)
    kept = set(out['stock_code'])
    assert '605366' not in kept and '600520' not in kept and '002668' not in kept, kept
    assert '603977' in kept and '002225' in kept, kept

def test_gate_window_insufficient_excluded():
    # 不存在的 code(无历史)→ 窗口不足 → 剔除
    cand = pd.DataFrame({'stock_code': ['ZZZNONE'], 'change_pct': [9.0]})
    out = f._filter_by_w2s01_gate(cand, '20260626', gate=50)
    assert len(out) == 0, out['stock_code'].tolist()

def test_empty_candidates_passthrough():
    out = f._filter_by_w2s01_gate(pd.DataFrame(), '20260626', gate=50)
    assert out is None or len(out) == 0

if __name__ == '__main__':
    test_gate_filters_pseudo_weak()
    test_gate_window_insufficient_excluded()
    test_empty_candidates_passthrough()
    print('PASS test_w2s01_gate')
```

- [ ] **Step 2: 运行确认失败**

Run: `cd 06-最终成果/模型原型/src && python3 tests/test_w2s01_gate.py 2>&1 | grep -v NotOpenSSL`
Expected: FAIL — `AttributeError: 'IntegratedDataFetcher' object has no attribute '_filter_by_w2s01_gate'`

- [ ] **Step 3: `__init__` 末尾追加配置 + 实例**

在 `IntegratedDataFetcher.__init__`(134 行起)末尾(`self.cache = self.db_manager.cache` 一行,156 行之后)追加:
```python
        # 弱转强准入门槛(spec 13):gate 配置化,实例复用改进版 W2S01
        import os
        self.w2s01_gate = float(os.environ.get('QSS_W2S01_GATE', '50'))
        from factors.w2s_daily_factors import DailyW2SFactors
        self._w2s_calc = DailyW2SFactors()
```

- [ ] **Step 4: 新增 `_filter_by_w2s01_gate` 方法**(置于 `_compute_volume_ratio` 之后、`_rank_by_leader_strength` 之前)

```python
    def _filter_by_w2s01_gate(self, candidates, trade_date, gate):
        """弱转强准入门槛:保留 W2S01>=gate 的候选(保序)。
        窗口不足(<L+1)/ W2S01 为 None / 单票查询失败 → 剔除;整库无路径 → 跳过门槛。
        仿 _compute_volume_ratio 的 sqlite 直连逐票取数。"""
        import sqlite3
        if candidates is None or len(candidates) == 0:
            return candidates
        L = self._w2s_calc.lookback_L
        db = getattr(self.stock_store, 'db_path', None) or getattr(self, 'db_path', None)
        if not db:
            logger.warning("[W2S01门槛] 无数据库路径,跳过门槛")
            return candidates
        keep = []
        conn = sqlite3.connect(db)
        conn.execute('PRAGMA busy_timeout=8000')
        try:
            for code in candidates['stock_code']:
                try:
                    rows = conn.execute(
                        "SELECT close_price,volume,ma20,ma5,change_pct FROM stock_kline "
                        "WHERE stock_code=? AND trade_date<=? ORDER BY trade_date DESC LIMIT ?",
                        (code, trade_date, L + 1)).fetchall()
                except Exception:
                    continue  # 单票查询失败 → 剔除
                if len(rows) < L + 1:
                    continue  # 窗口不足 → 剔除
                rows = rows[::-1]  # oldest→newest
                today = rows[-1]
                window = [{'close': r[0], 'volume': r[1], 'ma20': r[2]} for r in rows[:-1]]
                td = {'change_pct': today[4], 'volume': today[1], 'close': today[0],
                      'ma5': today[3], 'window_klines': window}
                w = self._w2s_calc.calculate_W2S01(td, {})
                if w is not None and w >= gate:
                    keep.append(code)
        finally:
            conn.close()
        before = len(candidates)
        out = candidates[candidates['stock_code'].isin(keep)].copy()
        logger.info(f"[W2S01门槛] gate={gate} 候选 {before}→{len(out)} 只(剔除非弱转强 {before - len(out)})")
        if len(out) == 0:
            logger.warning(f"[W2S01门槛] {trade_date} 门槛后无候选(无 W2S01≥{gate} 的弱转强票)")
        return out
```

- [ ] **Step 5: 接入 `_filter_active_from_kline`**

把(455-458 行):
```python
        candidates = df[df['change_pct'] >= min_change_pct].copy()
        if candidates.empty:
            return pd.DataFrame()
        active_df = self._rank_by_leader_strength(candidates, trade_date).head(top_n)
```
改为:
```python
        candidates = df[df['change_pct'] >= min_change_pct].copy()
        if candidates.empty:
            return pd.DataFrame()
        # 弱转强准入门槛(spec 13):涨幅≥5% AND W2S01≥gate 才进入排序
        candidates = self._filter_by_w2s01_gate(candidates, trade_date, self.w2s01_gate)
        if candidates is None or candidates.empty:
            return pd.DataFrame()
        active_df = self._rank_by_leader_strength(candidates, trade_date).head(top_n)
```

- [ ] **Step 6: 运行确认通过**

Run: `cd 06-最终成果/模型原型/src && python3 tests/test_w2s01_gate.py 2>&1 | grep -v NotOpenSSL | grep -v warnings.warn | tail -3`
Expected: `PASS test_w2s01_gate`(605366/600520/002668 被剔除,603977/002225 保留,不存在 code 剔除)

- [ ] **Step 7: 提交**

```bash
git add -f 06-最终成果/模型原型/src/data_fetcher_v3_integrated.py 06-最终成果/模型原型/src/tests/test_w2s01_gate.py
git commit -m "feat(w2s): 弱转强准入门槛 W2S01>=gate(默认50)接入活跃池构建"
```

---

### Task 2: daily 端到端 + 回测验证

**Files:**
- 无新增代码;运行验证。

**Interfaces:**
- Consumes: Task 1 的门槛;`stock_history.db`(已同步至 20260626)。

- [ ] **Step 1: daily 端到端——新 Top5 每只 W2S01≥50**

Run: `cd 06-最终成果/模型原型/src && caffeinate -i python3 cli.py --date 20260626 --mode daily 2>&1 | grep -v NotOpenSSL | grep -viE "因子计算|INFO:factors" | grep -iE "W2S01门槛|推荐股票|得分|top_picks" | tail -20`
Expected: 日志含 `[W2S01门槛] gate=50 候选 N→M`;产出 Top(可能少于5);读 `output/reports/pre_market_20260626.md` 核对推荐票的 W2S01 列**均 ≥50**(此前 605366=28.4/600520=32.3/002668=38.1 应不再出现在推荐)。

- [ ] **Step 2: 回测北极星不回归**

Run: `cd 06-最终成果/模型原型/src && python3 _s1_verify.py 20260101 20260331 2>&1 | grep -v NotOpenSSL | grep -v warnings.warn | tail -14`
Expected: `北极星验收 : PASS ✓ (非零交易)`;记录交易数/盈亏比(门槛收紧候选池,预期交易数下降、弱转强纯度上升;对比无门槛基线 33笔/盈亏比1.05)。

- [ ] **Step 3: 记录 + 收尾**

```bash
git commit --allow-empty -m "test(w2s): 弱转强门槛验收 daily新Top全W2S01≥50 + 回测trades>0"
```

- [ ] **Step 4: finishing-a-development-branch**

Announce 并走 superpowers:finishing-a-development-branch 收尾(验证测试、呈现合并选项)。

---

## Self-Review

- **Spec 覆盖**:spec §3.1 接入点→Task1 Step5;§3.2 方法→Task1 Step4;§3.4 复用+实例→Task1 Step3;§4 gate配置→Task1 Step3;§5 Fail-Loud/软处理(窗口不足/查询失败/无路径/空)→Task1 Step4 全覆盖+测试;§6 验证→Task1 集成测试 + Task2 daily/回测。
- **占位符**:无 TBD;每步含可执行代码/命令。
- **类型一致**:`_filter_by_w2s01_gate(candidates: DataFrame, trade_date: str, gate: float) -> DataFrame`;`self.w2s01_gate: float`;`self._w2s_calc: DailyW2SFactors`;`calculate_W2S01` 返回 float|None,门槛 `w is not None and w >= gate` 处理跨 Task1 一致。
- **YAGNI**:市值门槛/数据补全/gate寻优不在本计划(spec §7)。
