# W2S02/W2S03 乘性化 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `calculate_W2S02`(量价配合)与 `calculate_W2S03`(均线趋势)从满分泛滥的二值/加性逻辑重写为连续乘性打分,根治综合分同质,使 W2S01/02/03 三因子信息正交。

**Architecture:** 在 `DailyW2SFactors` 内新增私有连续打分子函数(复用已有 `_trapezoid` 与 clamp 模式),`calculate_W2S02/03` 检测所需字段:齐全→乘性路径返 `100×A×B`;缺失→回落现有旧逻辑(调用方零改动)。W2S03 需 `today_data` 补 ma5/10/20 数值(接线),W2S02 零接线(字段已齐)。

**Tech Stack:** Python 3.9(系统解释器,无 venv);纯 stdlib;测试用 standalone `python3 tests/xxx.py`(无 pytest)。

## Global Constraints
- 语言:Python 3.9 兼容(避免 `match`、`X | Y` 联合类型)。
- Fail-Loud(项目§四):子项字段缺失→同侧权重重归一化;整侧不可算→返 **None**(由 `signal_generator_v4` 剔除重归一化);禁止用中性值伪造;不抛异常到上层。
- 非破坏:`calculate_W2S02/03(today_data, yesterday_data)` 签名不变;新字段为 `today_data` 可选键;字段缺失回落旧逻辑。
- 测试方式:`python3 tests/<file>.py`(无 pytest);`assert` 断言,文件可直接执行。
- 参数可配:经 `self.config.get(...)`,初值见各任务。
- 缺失判定统一用 `is not None`(避免 0.0 被当缺失,与 `_weak_score`/`_strong_score` 口径一致)。
- 基线分支 `feature/w2s01-improvement`;`_bmad-output` 在 .gitignore,提交用 `git add -f`。
- 提交 message 结尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

### Task 1: W2S02 量价配合(`_vol_burst` + `_price_confirm` + 重写 `calculate_W2S02`)

**Files:**
- Modify: `factors/w2s_daily_factors.py`(`__init__` 末尾加配置;`calculate_W2S01` 前后新增两个私有方法;重写 `calculate_W2S02:233-283` 方法体)
- Test: `tests/test_w2s02_multiplicative.py`(新建)

**Interfaces:**
- Consumes: `today_data` 含 `open`/`high`/`low`/`close`/`volume`/`window_klines`(后者计划2 已注入,项含 `volume`)。
- Produces:
  - `_vol_burst(today: dict, window: list) -> float | None`:今量/窗口均量 clamp 到 [0,1];today.volume 缺或窗口无有效量或均量≤0 返 None。
  - `_price_confirm(today: dict) -> float | None`:实体多头度×w + 收盘强势度×w → [0,1];open/high/low/close 任一缺失返 None。
  - `calculate_W2S02` 行为升级:`today_data` 含 open 且有有效 window → `100×vol_burst×price_confirm`(任一 None 返 None);否则回落旧逻辑。

- [ ] **Step 1: 写失败测试** `tests/test_w2s02_multiplicative.py`

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from factors.w2s_daily_factors import DailyW2SFactors

f = DailyW2SFactors()

def _win(vols):
    return [{'close': 10.0, 'volume': v, 'ma20': 9.0} for v in vols]

def test_volume_breakout_bald_yang_high():
    # 放量3倍、光头大阳(收在最高、实体大) → 高分
    today = {'open': 10.0, 'close': 10.9, 'high': 10.9, 'low': 10.0,
             'volume': 300, 'change_pct': 9.0, 'window_klines': _win([100]*5)}
    s = f.calculate_W2S02(today, {})
    assert s is not None and s >= 60, s

def test_volume_long_upper_shadow_low():
    # 放量3倍但收长上影(冲高回落,收在低位) → 价格不确认 → 低分
    today = {'open': 10.0, 'close': 10.1, 'high': 11.0, 'low': 10.0,
             'volume': 300, 'change_pct': 1.0, 'window_klines': _win([100]*5)}
    s = f.calculate_W2S02(today, {})
    assert s is not None and s <= 25, s

def test_volume_yin_near_zero():
    # 放量但收阴(实体为负、收在低位) → price_confirm≈0 → ≈0
    today = {'open': 10.5, 'close': 10.0, 'high': 10.6, 'low': 10.0,
             'volume': 300, 'change_pct': -2.0, 'window_klines': _win([100]*5)}
    s = f.calculate_W2S02(today, {})
    assert s is not None and s <= 10, s

def test_no_volume_shrink_low():
    # 缩量(今量<窗口均量) → vol_burst≈0 → 低分
    today = {'open': 10.0, 'close': 10.9, 'high': 10.9, 'low': 10.0,
             'volume': 80, 'change_pct': 9.0, 'window_klines': _win([100]*5)}
    s = f.calculate_W2S02(today, {})
    assert s is not None and s <= 10, s

def test_limit_up_one_word_no_divzero():
    # 一字涨停 high==low,closepos 守卫=1,不除零
    today = {'open': 11.0, 'close': 11.0, 'high': 11.0, 'low': 11.0,
             'volume': 300, 'change_pct': 10.0, 'window_klines': _win([100]*5)}
    s = f.calculate_W2S02(today, {})
    assert s is not None and s >= 30, s

def test_backward_compat_no_window():
    # 无 window_klines + 无 open → 回落旧逻辑(昨弱今强放量 base)
    today = {'change_pct': 6.0, 'volume': 300}
    yest = {'change_pct': 1.0, 'volume': 100}
    s = f.calculate_W2S02(today, yest)
    assert s is not None and s > 0

if __name__ == '__main__':
    test_volume_breakout_bald_yang_high(); test_volume_long_upper_shadow_low()
    test_volume_yin_near_zero(); test_no_volume_shrink_low()
    test_limit_up_one_word_no_divzero(); test_backward_compat_no_window()
    print('PASS test_w2s02_multiplicative')
```

- [ ] **Step 2: 运行确认失败**

Run: `cd 06-最终成果/模型原型/src && python3 tests/test_w2s02_multiplicative.py`
Expected: FAIL — `test_volume_breakout_bald_yang_high` 断言失败(实现前 `calculate_W2S02` 走旧逻辑:`yesterday_data={}` 致 `yesterday_vol=0` 不进放量分支、返 0 < 60)。

- [ ] **Step 3: `__init__` 末尾追加 W2S02 配置**

在 `__init__` 末尾(`self.vr_span = ...` 行后)追加:
```python
        # W2S02 量价配合参数(本计划)
        self.w2s02_vr_span = self.config.get('w2s02_vr_span', 1.5)      # 量比满分跨度(vr=2.5封顶)
        self.w2s02_body_span = self.config.get('w2s02_body_span', 0.05)  # 实体多头度满分跨度
        self.w2s02_pc_w = self.config.get('w2s02_pc_weights', (0.5, 0.5))  # (实体, 收盘强势)
```

- [ ] **Step 4: 新增两个私有方法**(在 `_strong_score` 之后、`calculate_W2S01` 之前)

```python
    def _vol_burst(self, today, window):
        """量能爆发门控[0,1]:今量/窗口均量,clamp((vr-1)/vr_span,0,1)。不可算返None。"""
        tv = today.get('volume')
        vols = [w.get('volume') for w in (window or []) if w.get('volume') is not None]
        if tv is None or not vols:
            return None
        avg_v = sum(vols) / len(vols)
        if avg_v <= 0:
            return None
        vr = tv / avg_v
        return max(0.0, min(1.0, (vr - 1.0) / self.w2s02_vr_span))

    def _price_confirm(self, today):
        """价格确认度[0,1]:实体多头度 + 收盘强势度加权。关键字段缺失返None。"""
        o = today.get('open'); c = today.get('close')
        h = today.get('high'); l = today.get('low')
        if o is None or c is None or h is None or l is None:
            return None
        parts, weights = [], []
        # ① 实体多头度:收阳实体越大越高,收阴→0
        if o > 0:
            body = max(0.0, min(1.0, (c - o) / o / self.w2s02_body_span))
            parts.append(body); weights.append(self.w2s02_pc_w[0])
        # ② 收盘强势度:(close-low)/(high-low);一字板 high==low 守卫=1
        closepos = 1.0 if h <= l else max(0.0, min(1.0, (c - l) / (h - l)))
        parts.append(closepos); weights.append(self.w2s02_pc_w[1])
        wsum = sum(weights)
        return sum(p * w for p, w in zip(parts, weights)) / wsum if wsum > 0 else None
```

- [ ] **Step 5: 重写 `calculate_W2S02` 方法体**(在 `try:` 之后、原 `score = 0` 之前插入乘性路径;原旧逻辑整体保留为回落)

在 `calculate_W2S02` 的 `try:` 行之后插入:
```python
            # 改进版:有量价字段 + 有效窗口 → 连续乘性打分(量价配合)
            window = today_data.get('window_klines')
            if window and len(window) >= self.lookback_L and today_data.get('open') is not None:
                vb = self._vol_burst(today_data, window)
                pc = self._price_confirm(today_data)
                if vb is None or pc is None:
                    return None   # 整侧不可算 → Fail-Loud None
                return round(100.0 * vb * pc, 2)
            # 否则回落原两日逻辑(向后兼容)↓↓↓(保留下方原有代码不动)
```

- [ ] **Step 6: 运行确认通过 + W2S01 单测不回归**

Run: `cd 06-最终成果/模型原型/src && python3 tests/test_w2s02_multiplicative.py`
Expected: `PASS test_w2s02_multiplicative`
Run: `python3 tests/test_w2s01_integration.py && python3 tests/test_w2s01_weak_score.py && python3 tests/test_w2s01_strong_score.py`
Expected: 三个 PASS

- [ ] **Step 7: 提交**

```bash
cd <repo-root>
git add -f 06-最终成果/模型原型/src/factors/w2s_daily_factors.py 06-最终成果/模型原型/src/tests/test_w2s02_multiplicative.py
git commit -m "feat(w2s): W2S02 量价配合乘性化(vol_burst×price_confirm)+回落旧逻辑"
```
> 注:`<repo-root>` = 项目根 `Research_Team_Assembler`;实际提交路径前缀为 `_bmad-output/research-projects/RF-2026-A-Stock-Selection-Model/`。下同。

---

### Task 2: W2S03 均线趋势(`_trend_align` + `_ma_position` + 重写 `calculate_W2S03`)

**Files:**
- Modify: `factors/w2s_daily_factors.py`(`__init__` 末尾加配置;`_price_confirm` 后新增两个私有方法;重写 `calculate_W2S03:285-339` 方法体)
- Test: `tests/test_w2s03_multiplicative.py`(新建)

**Interfaces:**
- Consumes: `today_data` 含 `close`/`ma5`/`ma10`/`ma20`(数值;ma 由 Task3 接线注入)。
- Produces:
  - `_trend_align(ma5, ma10, ma20) -> float | None`:相邻均线间距连续打分 [0,1];均线全缺返 None。
  - `_ma_position(close, ma5, ma10, ma20) -> float | None`:close 相对各均线距离加权 [0,1];close 缺或均线全缺返 None。
  - `calculate_W2S03` 行为升级:`today_data` 含 ma5/ma10/ma20 数值 → `100×trend_align×ma_position`(任一 None 返 None);否则回落旧逻辑(above_maX 二值)。

- [ ] **Step 1: 写失败测试** `tests/test_w2s03_multiplicative.py`

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from factors.w2s_daily_factors import DailyW2SFactors

f = DailyW2SFactors()

def test_perfect_bull_high():
    # 完美多头发散 ma5>ma10>ma20 间距充分 + 收盘站稳三线上方
    today = {'close': 10.5, 'ma5': 10.2, 'ma10': 9.9, 'ma20': 9.6, 'change_pct': 5.0}
    s = f.calculate_W2S03(today, {})
    assert s is not None and s >= 60, s

def test_bear_alignment_near_zero():
    # 空头排列 ma5<ma10<ma20 + 收盘跌破 → ≈0
    today = {'close': 9.0, 'ma5': 9.3, 'ma10': 9.6, 'ma20': 9.9, 'change_pct': -3.0}
    s = f.calculate_W2S03(today, {})
    assert s is not None and s <= 10, s

def test_entangled_mid():
    # 均线纠缠(间距≈0)、收盘刚好贴线 → 中间分(不满不空)
    today = {'close': 10.0, 'ma5': 10.0, 'ma10': 9.99, 'ma20': 9.98, 'change_pct': 1.0}
    s = f.calculate_W2S03(today, {})
    assert s is not None and 0 < s < 40, s

def test_continuity_no_cliff():
    # close 微变,分数平滑(无二值悬崖)
    base = {'ma5': 10.2, 'ma10': 9.9, 'ma20': 9.6, 'change_pct': 5.0}
    s1 = f.calculate_W2S03({**base, 'close': 10.49}, {})
    s2 = f.calculate_W2S03({**base, 'close': 10.51}, {})
    assert abs(s1 - s2) < 5, (s1, s2)

def test_backward_compat_no_ma_values():
    # 无 ma 数值、有 above_maX 二值 → 回落旧逻辑
    today = {'above_ma5': True, 'above_ma10': True, 'above_ma20': False, 'change_pct': 5.0}
    yest = {'above_ma5': False, 'above_ma10': False, 'above_ma20': False}
    s = f.calculate_W2S03(today, yest)
    assert s is not None and s > 0

if __name__ == '__main__':
    test_perfect_bull_high(); test_bear_alignment_near_zero()
    test_entangled_mid(); test_continuity_no_cliff(); test_backward_compat_no_ma_values()
    print('PASS test_w2s03_multiplicative')
```

- [ ] **Step 2: 运行确认失败**

Run: `cd 06-最终成果/模型原型/src && python3 tests/test_w2s03_multiplicative.py`
Expected: FAIL — `test_bear_alignment_near_zero` 断言失败(旧逻辑看 above_maX 二值,本输入无该键当 False,score=0 偶然过;`test_perfect_bull_high` 必失,旧逻辑无 ma 数值路径返回低分)。

- [ ] **Step 3: `__init__` 末尾追加 W2S03 配置**

在 `__init__` 末尾(W2S02 配置行后)追加:
```python
        # W2S03 均线趋势参数(本计划)
        self.w2s03_align_span = self.config.get('w2s03_align_span', 0.02)  # 均线间距满分跨度
        self.w2s03_pos_span = self.config.get('w2s03_pos_span', 0.03)      # 站位满分跨度
        self.w2s03_pos_w = self.config.get('w2s03_pos_weights', (0.5, 0.3, 0.2))  # (ma5,ma10,ma20)
```

- [ ] **Step 4: 新增两个私有方法**(在 `_price_confirm` 之后)

```python
    def _trend_align(self, ma5, ma10, ma20):
        """多头排列度[0,1]:相邻均线间距连续打分。均线全缺返None。"""
        S = self.w2s03_align_span
        parts = []
        if ma5 is not None and ma10 is not None and ma10 > 0:
            parts.append(max(0.0, min(1.0, (ma5 - ma10) / ma10 / S)))
        if ma10 is not None and ma20 is not None and ma20 > 0:
            parts.append(max(0.0, min(1.0, (ma10 - ma20) / ma20 / S)))
        if not parts:
            return None
        return sum(parts) / len(parts)

    def _ma_position(self, close, ma5, ma10, ma20):
        """站位度[0,1]:close相对各均线距离加权。close缺或均线全缺返None。"""
        if close is None:
            return None
        P = self.w2s03_pos_span
        parts, weights = [], []
        for ma, wi in zip((ma5, ma10, ma20), self.w2s03_pos_w):
            if ma is not None and ma > 0:
                parts.append(max(0.0, min(1.0, (close - ma) / ma / P)))
                weights.append(wi)
        wsum = sum(weights)
        return sum(p * w for p, w in zip(parts, weights)) / wsum if wsum > 0 else None
```

- [ ] **Step 5: 重写 `calculate_W2S03` 方法体**(在 `try:` 之后、原 `score = 0` 之前插入乘性路径;原旧逻辑整体保留为回落)

在 `calculate_W2S03` 的 `try:` 行之后插入:
```python
            # 改进版:有 ma 数值 → 连续乘性打分(均线趋势结构)
            ma5 = today_data.get('ma5'); ma10 = today_data.get('ma10'); ma20 = today_data.get('ma20')
            close = today_data.get('close')
            if ma5 is not None and ma10 is not None and ma20 is not None and close is not None:
                ta = self._trend_align(ma5, ma10, ma20)
                mp = self._ma_position(close, ma5, ma10, ma20)
                if ta is None or mp is None:
                    return None   # 整侧不可算 → Fail-Loud None
                return round(100.0 * ta * mp, 2)
            # 否则回落原 above_maX 二值逻辑(向后兼容)↓↓↓(保留下方原有代码不动)
```

- [ ] **Step 6: 运行确认通过 + 不回归**

Run: `cd 06-最终成果/模型原型/src && python3 tests/test_w2s03_multiplicative.py`
Expected: `PASS test_w2s03_multiplicative`
Run: `python3 tests/test_w2s02_multiplicative.py && python3 tests/test_w2s01_integration.py`
Expected: 两个 PASS

- [ ] **Step 7: 提交**

```bash
git add -f 06-最终成果/模型原型/src/factors/w2s_daily_factors.py 06-最终成果/模型原型/src/tests/test_w2s03_multiplicative.py
git commit -m "feat(w2s): W2S03 均线趋势乘性化(trend_align×ma_position)+回落旧逻辑"
```

---

### Task 3: 接线 `today_data` 补 ma5/10/20 数值(选股+回测)+ 真实数据冒烟

**Files:**
- Modify: `main.py`(选股路径 today_data 构造,多日分支 ~309 后 与 单日分支 ~323;在计划2 `window_klines` 注入附近)
- Modify: `backtest_framework.py`(回测路径 today_data 构造 ~307 后)
- Test: `tests/test_w2s0203_smoke_realdata.py`(新建)

**Interfaces:**
- Consumes: `kline_df`(已 sort_values('trade_date') 升序,含列 `ma5`/`ma10`/`ma20`/`open_price`/`high_price`/`low_price`)。
- Produces: `today_data` 新增键 `ma5`/`ma10`/`ma20`(float),激活 W2S03 乘性路径。

- [ ] **Step 1: 写冒烟脚本** `tests/test_w2s0203_smoke_realdata.py`

```python
import sys, os, sqlite3
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from factors.w2s_daily_factors import DailyW2SFactors
f = DailyW2SFactors(); L = f.lookback_L
c = sqlite3.connect('data/stock_history.db')
codes = [r[0] for r in c.execute(
    'SELECT DISTINCT stock_code FROM stock_kline WHERE trade_date="20260626" LIMIT 200')]
s02, s03 = [], []
for code in codes:
    rows = c.execute('SELECT close_price,volume,ma20,change_pct,ma5,ma10,open_price,high_price,low_price '
                     'FROM stock_kline WHERE stock_code=? AND trade_date<="20260626" '
                     'ORDER BY trade_date DESC LIMIT ?', (code, L + 1)).fetchall()
    if len(rows) < L + 1:
        continue
    rows = rows[::-1]  # oldest->newest
    today = rows[-1]; window = [{'close': r[0], 'volume': r[1], 'ma20': r[2]} for r in rows[:-1]]
    td = {'close': today[0], 'volume': today[1], 'change_pct': today[3],
          'ma5': today[4], 'ma10': today[5], 'open': today[6], 'high': today[7], 'low': today[8],
          'ma20': today[2], 'window_klines': window}
    v02 = f.calculate_W2S02(td, {}); v03 = f.calculate_W2S03(td, {})
    if v02 is not None: s02.append(v02)
    if v03 is not None: s03.append(v03)
def report(name, xs):
    full = sum(1 for s in xs if s >= 99)
    print(f'{name}: 样本={len(xs)} 满分={full} 均值={sum(xs)/max(len(xs),1):.1f} max={max(xs) if xs else 0:.1f}')
    assert len(xs) > 50, f'{name} 样本过少'
    assert full < len(xs) * 0.3, f'{name} 满分泛滥未改善'
report('W2S02', s02); report('W2S03', s03)
print('PASS smoke_w2s0203')
```

- [ ] **Step 2: 运行冒烟(逻辑层真实数据回归)**

> 说明:冒烟脚本自带 ma5/10/20 数值,直接走 Task1/2 已实现的乘性路径,验证 W2S02/W2S03 在真实数据分布满分占比 <30%。Step3-4 的接线是让 `main.py`/`backtest` 真实选股/回测路径也能激活 W2S03(端到端生效由 Task4 daily 验证)。

Run: `cd 06-最终成果/模型原型/src && python3 tests/test_w2s0203_smoke_realdata.py 2>&1 | grep -v NotOpenSSL | grep -v warnings.warn`
Expected: 打印两行分布 + `PASS smoke_w2s0203`(W2S02/W2S03 满分占比均 <30%)。若某项满分≥30%,回 Task1/2 调阈值。

- [ ] **Step 3: main.py 多日分支补 ma 数值**

定位计划2 注入 `window_klines` 的代码块(`today_data['window_klines'] = [...]` 紧邻),在其 today_data 已构造的多日分支内,`today_data` 字典中补三键。在 main.py 多日分支 today_data 字典(含 `'above_ma20': ...`)末尾追加:
```python
                                'ma5': float(today_row.get('ma5', 0) or 0),
                                'ma10': float(today_row.get('ma10', 0) or 0),
                                'ma20': float(today_row.get('ma20', 0) or 0),
```
> 注:旧键 `above_ma5/10/20` 保留不动(回落路径仍需);新增 ma 数值键供乘性路径。`0` 值时 `_trend_align`/`_ma_position` 的 `ma>0` 守卫会剔除该子项(新股不足均线周期 → 安全降级)。

- [ ] **Step 4: backtest_framework.py 补 ma 数值**

在 backtest_framework.py today_data 字典(含 `'above_ma20': ...`)末尾追加同样三行:
```python
                        'ma5': float(today_row.get('ma5', 0) or 0),
                        'ma10': float(today_row.get('ma10', 0) or 0),
                        'ma20': float(today_row.get('ma20', 0) or 0),
```

- [ ] **Step 5: 语法检查 + 冒烟通过**

Run: `cd 06-最终成果/模型原型/src && python3 -m py_compile main.py backtest_framework.py && echo OK`
Expected: `OK`
Run: `python3 tests/test_w2s0203_smoke_realdata.py 2>&1 | grep -v NotOpenSSL | grep -v warnings.warn`
Expected: 两行分布 + `PASS smoke_w2s0203`(W2S02/W2S03 满分占比均 <30%)

- [ ] **Step 6: 提交**

```bash
git add -f 06-最终成果/模型原型/src/main.py 06-最终成果/模型原型/src/backtest_framework.py 06-最终成果/模型原型/src/tests/test_w2s0203_smoke_realdata.py
git commit -m "feat(w2s): today_data接线补ma5/10/20数值激活W2S03+真实数据冒烟"
```

---

### Task 4: 回归回测 trades>0 + 端到端 daily 综合分离散度验证

**Files:**
- 无新增代码;运行验证 + 记录结果。

**Interfaces:**
- Consumes: 全部前序改动;`stock_history.db` 真实数据(已同步至 20260626)。

- [ ] **Step 1: 回归回测北极星**

Run: `cd 06-最终成果/模型原型/src && python3 _s1_verify.py 20260101 20260331 2>&1 | grep -v NotOpenSSL | grep -v warnings.warn | tail -15`
Expected: `北极星验收 : PASS ✓ (非零交易)`,记录 交易数/胜率/盈亏比/收益(与计划2 基线 29笔/1.56 对比)。

- [ ] **Step 2: 端到端 daily 离散度验证**

Run: `cd 06-最终成果/模型原型/src && caffeinate -i python3 cli.py --date 20260626 --mode daily 2>&1 | grep -v NotOpenSSL | grep -viE "因子计算|INFO:factors" | tail -25`
Expected: 跑通产出 Top5;读 `output/reports/pre_market_20260626.md`,**核对 W2S02/W2S03 列不再一片 99-100、综合分不再挤在 54-57**(区分度恢复)。

- [ ] **Step 3: 记录对比 + 收尾**

把 Step1/Step2 的关键指标(回测盈亏比、Top5 综合分范围/标准差 vs 改进前 54-57)写入提交说明。

```bash
git commit --allow-empty -m "test(w2s): W2S02/03乘性化验收 回测trades>0 + daily综合分离散度恢复"
```

- [ ] **Step 4: finishing-a-development-branch**

Announce: "I'm using the finishing-a-development-branch skill to complete this work." 走该 skill 收尾(验证测试、呈现合并/PR 选项)。

---

## Self-Review

- **Spec 覆盖**:spec §4(W2S02)→Task1;§5(W2S03)→Task2;§6 接线→Task3;§8 验证四道→Task1-2 单测 + Task3 冒烟 + Task4 回测/daily;§7 配置项→Task1/2 Step3。无遗漏。
- **占位符**:无 TBD;每步含可执行代码/命令。
- **类型一致**:`_vol_burst`/`_price_confirm`/`_trend_align`/`_ma_position` 均返 `float|None`;`calculate_W2S02/03` 对 None 返 None;today_data 键 `open/high/low/close/volume/ma5/ma10/ma20/window_klines` 跨 Task1-3 一致。
- **非破坏验证**:Task1/2 各含 `test_backward_compat_*`,确保无对应字段时回落旧逻辑。
- **YAGNI**:W2S04-06、龙虎榜、IC加权、乖离惩罚均不在本计划(spec §11)。
